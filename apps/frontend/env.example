# Local dev (optional): same machine / LAN — API on :3000, WebSocket on :5000
# VITE_API_HOST=192.168.1.10

# Production (Vercel): point to your EC2 API and WebSocket hosts (HTTPS / WSS recommended).
# VITE_API_BASE_URL=https://api.yourdomain.com
# VITE_WS_BASE_URL=wss://ws.yourdomain.com
