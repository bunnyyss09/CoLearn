REDIS_URL=redis://:PASSWORD@your-redis-host:6379

# Listen port (often 5000; map 443 -> 5000 behind ALB/nginx if you use wss://).
PORT=5000

# Set variables with systemd, PM2, or `export` before `node dist/index.js` (this app does not load a .env file).
